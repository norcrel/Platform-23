using UnityEngine;
using System.Collections;

public class inputTest : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
		//Debug.Log ("1: " + Input.GetAxis ("Horizontal") + ", " + Input.GetAxis ("Vertical"));
		//Debug.Log ("2: " + Input.GetAxis ("Horizontal2") + ", " + Input.GetAxis ("Vertical2"));
	}
}
